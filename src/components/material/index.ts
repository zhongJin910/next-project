import { styled } from "@mui/material/styles";
import {
  Button,
  IconButton as MIconButton,
  Checkbox as MCheckbox,
} from "@mui/material";
const MButton = styled(Button)({
  textTransform: "none",
});

export { MButton, MIconButton, MCheckbox };
